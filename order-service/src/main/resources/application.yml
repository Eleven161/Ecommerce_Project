server:
  port: 8082
spring:  datasource:    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: root  jpa:
    hibernate:      ddl-auto: update
    show-sql: true  application:    name: ORDER-SERVICE  mvc:    pathmatch:      matching-strategy: ANT_PATH_MATCHER
  
#  mvc:
#      pathmatch:
#            matching-strategy: ant_path_matcher